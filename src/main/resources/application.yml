server:
  port: 8080

spring:
  datasource:
    url: '${DB_URL:jdbc:mysql://avnadmin:AVNS_HojgG_Lurgxf9EkKmCC@lecture-archive-db-snuti-exp-archive-server.b.aivencloud.com:21711/defaultdb?ssl-mode=REQUIRED}'
    username: '${DB_USER:avnadmin}'
    password: '${DB_PASSWORD:AVNS_HojgG_Lurgxf9EkKmCC}'
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: "${GMAIL_USERNAME:trailsnuti@gmail.com}"
    password: "${GMAIL_APP_PASSWORD:pakb ykvr fpep kekq}"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

cloud:
  aws:
    region: ap-northeast-2
    s3:
      bucket: snuti-exp-archive-images-2026

app:
  jwt:
    secret: ${JWT_SECRET:change-me-please-change-this-to-a-long-secret}
    issuer: lecture-archive
    access-token-exp-minutes: 60
  cdn:
    base-url: https://snuti-exp-archive-images-2026.s3.ap-northeast-2.amazonaws.com
