server:
  port: 8080

spring:
  datasource:
    url: '${DB_URL:jdbc:mysql://lecture-archive-db.ctomo8kma876.ap-northeast-2.rds.amazonaws.com:3306/lecture_archive?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul}'
    username: '${DB_USER:admin}'
    password: '${DB_PASSWORD:N|8~uzCmTaq2$y[ls3?T_CXRDV49}'
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: "${GMAIL_USERNAME:trailsnuti@gmail.com}"
    password: "${GMAIL_APP_PASSWORD:pakb ykvr fpep kekq}"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

cloud:
  aws:
    region: ap-northeast-2
    s3:
      bucket: snuti-exp-archive-images-2026

app:
  jwt:
    secret: ${JWT_SECRET:change-me-please-change-this-to-a-long-secret}
    issuer: lecture-archive
    access-token-exp-minutes: 60
  cdn:
    base-url: https://snuti-exp-archive-images-2026.s3.ap-northeast-2.amazonaws.com
